import 'package:flutter/material.dart';

class HSpace extends SizedBox {
  const HSpace(double height) : super(height: height, width: 0);
}

class WSpace extends SizedBox {
  const WSpace(double width) : super(height: 0, width: width);
}
